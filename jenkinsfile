pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }

        stage('build'){
             steps{
                 echo 'building the application'
             }
    }
        stage('deploy'){
            steps{
                echo 'deploy the application'
            }
        }
}
}
